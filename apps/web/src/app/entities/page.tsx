/**
 * Entity browser — people, projects, companies, topics, places
 *
 * Browse entities, view linked items, manage aliases.
 */

export default function EntitiesPage() {
  return (
    <main className="max-w-3xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Entities</h1>
      {/* TODO: Entity list + detail view + relationship graph */}
      <p className="text-gray-400">Entity browser — scaffold ready.</p>
    </main>
  );
}
