/**
 * Settings page — configurable via settings table
 *
 * All settings editable here write to the settings table.
 * Provider changes show "restart to apply" banner.
 */

export default function SettingsPage() {
  return (
    <main className="max-w-3xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Settings</h1>
      {/* TODO: Settings form bound to settings table */}
      <p className="text-gray-400">Settings page — scaffold ready.</p>
    </main>
  );
}
