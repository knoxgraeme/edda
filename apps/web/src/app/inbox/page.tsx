/**
 * Notification inbox — pending confirmations
 *
 * Shows all items where confirmed=false.
 * Approve, reject, or approve-all actions.
 */

export default function InboxPage() {
  return (
    <main className="max-w-3xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Inbox</h1>
      {/* TODO: Show pending confirmations with approve/reject actions */}
      <p className="text-gray-400">Notification inbox — scaffold ready.</p>
    </main>
  );
}
