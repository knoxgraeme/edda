[build]
buildCommand = "pnpm --filter @edda/db build && pnpm --filter @edda/web build"

[deploy]
startCommand = "pnpm --filter @edda/web start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
